#Start of Day 5

def main():
    open("/path/to/mars.jpg")

if __name__ == '__main__':
    main()

   
    #Tracebacks
    ''' when you run the above code '''
    '''  body of text that can point to the origin (and ending) of an unhandled error. '''
    '''  stdin (input in the interactive terminal) on the first line of the input.'''
    ''' FileNotFoundError (the exception name), which means that the file doesn't exist or perhaps the directory to it doesn't exist.'''